'use client';

// MapView is now managed directly in page.tsx via Leaflet dynamic import.
// This file is kept as a placeholder to avoid import errors.

export default function MapView() {
  return null;
}